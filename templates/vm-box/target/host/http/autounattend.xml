<?xml version="1.0" encoding="utf-8"?>
<!-- Answers the Windows installer so the build runs unattended, and enables
     WinRM so Packer can drive the machine afterwards. Without this the build
     hangs at the first dialog with nobody to click it.

     Adjust the image name to match your ISO's edition:
       dism /Get-WimInfo /WimFile:D:\sources\install.wim -->
<unattend xmlns="urn:schemas-microsoft-com:unattend">
  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <UserAccounts>
        <AdministratorPassword><Value>Packer!2026</Value><PlainText>true</PlainText></AdministratorPassword>
      </UserAccounts>
      <AutoLogon>
        <Password><Value>Packer!2026</Value><PlainText>true</PlainText></Password>
        <Enabled>true</Enabled><LogonCount>3</LogonCount><Username>Administrator</Username>
      </AutoLogon>
      <FirstLogonCommands>
        <SynchronousCommand wt:action="add" xmlns:wt="urn:schemas-microsoft-com:unattend">
          <CommandLine>powershell -Command "Set-ExecutionPolicy Bypass -Force; winrm quickconfig -quiet; winrm set winrm/config/service/Auth @{Basic=\"true\"}; winrm set winrm/config/service @{AllowUnencrypted=\"true\"}"</CommandLine>
          <Order>1</Order>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>
</unattend>
