#cloud-config
users:
  - name: destrier
    groups: [sudo]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    lock_passwd: false
    plain_text_passwd: destrier
ssh_pwauth: true
disable_root: true
